# Selector Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** ホームに 「Selector」 ボタンを追加し、 直近 30 日以内に DB へ追加された (dedup group 単位で MIN(created_at) 基準) アルバムからランダムに 1 枚を 「Selector カード」 として表示する。

**Architecture:** Domain に `SelectorCard` 型 + store に `pick_recent_addition` メソッド (CTE で dedup → MIN(created_at) で window フィルタ → RANDOM LIMIT 1 → json_group_array で同 group の rows をまとめて取得し head 行と find_map で組み立て)。 Leptos の Action で server fn を発火、 SelectorSlot は idle 時 DOM 不在で既存 grid のレイアウトに影響なし。

**Tech Stack:** Rust / Leptos 0.7 / sqlx (sqlite, offline mode) / chrono / Tailwind v4 / Playwright (visual regression — layout-only assertions、 pixel-diff ではない)

---

## File Structure

- **Create**: `src/domain/selector_card.rs` — `SelectorCard` domain 型
- **Modify**: `src/domain/mod.rs` — 新モジュールを export
- **Modify**: `src/server/store.rs` — `pick_recent_addition` メソッド + テスト
- **Modify**: `src/pages/home.rs` — `SelectorCardView` / `From` impl / `selector()` server fn / `SelectorButton` / `SelectorSlot` / `SelectorCard` コンポーネント / `Home` への統合
- **Auto-regenerated**: `.sqlx/` — `cargo sqlx prepare` 経由
- **No migration**: 既存 `recommendations.created_at` をそのまま使う

## Spec から判明した訂正

spec は 「実装 PR で `npx playwright test --update-snapshots` を 1 コミットに分離」 と書いているが、 `tests/visual/home.spec.ts` を確認したところ pixel-diff スナップショットは使っておらず、 CSS の `grid-template-columns` トークン数 + 要素 count + `aspect-ratio` 等の computed style 検証のみ。 `SelectorSlot` を idle 時 DOM 不在にすれば既存テストはそのままパスする想定なので、 スナップショット更新は不要。 (この訂正は Task 11 で実機確認する)

---

### Task 1: `SelectorCard` domain 型を新規追加

**Files:**
- Create: `src/domain/selector_card.rs`
- Modify: `src/domain/mod.rs`

- [ ] **Step 1: 失敗テストを書く**

`src/domain/selector_card.rs` を新規作成:

```rust
use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
pub struct SelectorCard {
    pub artist_name: String,
    pub album_name: Option<String>,
    pub spotify_url: Option<String>,
    pub spotify_image_url: Option<String>,
    pub youtube_url: Option<String>,
    /// dedup group の MIN(created_at)。 「うちが最初に拾った日」。
    pub added_at: DateTime<Utc>,
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn selector_card_holds_optional_fields_independently() {
        let now = Utc::now();
        let card = SelectorCard {
            artist_name: "Aldous Harding".into(),
            album_name: Some("Train on the Island".into()),
            spotify_url: Some("https://open.spotify.com/album/abc".into()),
            spotify_image_url: None,
            youtube_url: None,
            added_at: now,
        };
        assert_eq!(card.artist_name, "Aldous Harding");
        assert!(card.spotify_image_url.is_none());
    }
}
```

- [ ] **Step 2: モジュール export**

`src/domain/mod.rs` を修正 — 現状 `pub mod recommendation; pub mod album_card;` の 2 行のところに `pub mod selector_card;` を追加:

```rust
pub mod recommendation;
pub mod album_card;
pub mod selector_card;
```

- [ ] **Step 3: テストを通す**

Run: `cargo test --features ssr selector_card -- --nocapture`
Expected: PASS (1 test)

- [ ] **Step 4: コミット**

```bash
git add src/domain/selector_card.rs src/domain/mod.rs
git commit -m "feat(domain): SelectorCard 型を追加"
```

---

### Task 2: `pick_recent_addition` — window 内 1 件のみの最小ケース

**Files:**
- Modify: `src/server/store.rs`
- Auto-regenerated: `.sqlx/`

- [ ] **Step 1: 失敗テストを書く**

`src/server/store.rs` の `#[cfg(test)] mod tests` の末尾 (existing `list_recent_albums_orders_by_latest_featured_at_desc` の次) に追加。 ヘルパも置く:

```rust
async fn set_created_at(pool: &SqlitePool, id: i64, created_at: chrono::DateTime<chrono::Utc>) {
    sqlx::query("UPDATE recommendations SET created_at = ? WHERE id = ?")
        .bind(created_at)
        .bind(id)
        .execute(pool)
        .await
        .unwrap();
}

#[tokio::test]
async fn pick_recent_addition_returns_single_in_window_row() {
    use chrono::{Duration, Utc};
    let pool = setup_pool().await;
    let repo = RecommendationRepo::new(pool.clone());
    let (saved, _) = repo.upsert(sample_with(
        "rokinon", "a", "Aldous Harding", Some("Train on the Island"),
        Some("https://open.spotify.com/album/A"),
        NaiveDate::from_ymd_opt(2026, 5, 1).unwrap(),
    )).await.unwrap();
    set_created_at(&pool, saved.id, Utc::now() - Duration::days(5)).await;

    let since = Utc::now() - Duration::days(30);
    let card = repo.pick_recent_addition(since).await.unwrap().unwrap();
    assert_eq!(card.artist_name, "Aldous Harding");
    assert_eq!(card.album_name.as_deref(), Some("Train on the Island"));
}
```

- [ ] **Step 2: テスト失敗確認 (コンパイルエラー)**

Run: `cargo test --features ssr store::tests::pick_recent_addition_returns_single_in_window_row`
Expected: コンパイルエラー `no method named pick_recent_addition`

- [ ] **Step 3: 最小実装を追加**

`src/server/store.rs` の `impl RecommendationRepo` ブロック内 (existing `list_recent_albums` の下) に追加:

```rust
pub async fn pick_recent_addition(
    &self,
    since: chrono::DateTime<chrono::Utc>,
) -> AppResult<Option<crate::domain::selector_card::SelectorCard>> {
    use crate::domain::selector_card::SelectorCard;
    use crate::server::error::AppError;

    #[derive(serde::Deserialize)]
    struct RawRow {
        source_id: String,
        artist_name: String,
        album_name: Option<String>,
        spotify_url: Option<String>,
        spotify_image_url: Option<String>,
        youtube_url: Option<String>,
        featured_at: chrono::NaiveDate,
    }

    let row = sqlx::query!(
        r#"WITH keyed AS (
            SELECT
                COALESCE(
                    spotify_url,
                    lower(trim(artist_name)) || '|' || lower(trim(coalesce(album_name, '')))
                ) AS dedup_key,
                source_id,
                artist_name,
                album_name,
                spotify_url,
                spotify_image_url,
                youtube_url,
                featured_at,
                created_at
            FROM recommendations
        )
        SELECT
            dedup_key AS "dedup_key!: String",
            MIN(created_at) AS "added_at!: chrono::DateTime<chrono::Utc>",
            json_group_array(json_object(
                'source_id', source_id,
                'artist_name', artist_name,
                'album_name', album_name,
                'spotify_url', spotify_url,
                'spotify_image_url', spotify_image_url,
                'youtube_url', youtube_url,
                'featured_at', featured_at
            )) AS "rows_json!: String"
        FROM keyed
        GROUP BY dedup_key
        HAVING MIN(created_at) >= ?
        ORDER BY RANDOM()
        LIMIT 1"#,
        since,
    )
    .fetch_optional(&self.pool)
    .await?;

    let Some(row) = row else { return Ok(None); };
    let mut raw: Vec<RawRow> = serde_json::from_str(&row.rows_json)
        .map_err(|e| AppError::Parse(format!("rows_json: {e}")))?;
    raw.sort_by(|a, b| b.featured_at.cmp(&a.featured_at).then_with(|| a.source_id.cmp(&b.source_id)));
    let head = raw.first().ok_or_else(|| AppError::Parse("empty group".into()))?;
    Ok(Some(SelectorCard {
        artist_name: head.artist_name.clone(),
        album_name: raw.iter().find_map(|r| r.album_name.clone()),
        spotify_url: raw.iter().find_map(|r| r.spotify_url.clone()),
        spotify_image_url: raw.iter().find_map(|r| r.spotify_image_url.clone()),
        youtube_url: raw.iter().find_map(|r| r.youtube_url.clone()),
        added_at: row.added_at,
    }))
}
```

- [ ] **Step 4: sqlx の offline data を更新**

`.env` に `DATABASE_URL=sqlite:data/app.db` が設定済みで dev DB が migrate 済みであることを前提:

Run: `cargo sqlx prepare -- --features ssr`
Expected: `.sqlx/query-*.json` が複数生成される or 更新される。 失敗する場合は `mise run db:create && mise run db:migrate` を先に実行

- [ ] **Step 5: テストを通す**

Run: `cargo test --features ssr store::tests::pick_recent_addition_returns_single_in_window_row -- --nocapture`
Expected: PASS (1 test)

- [ ] **Step 6: コミット**

```bash
git add src/server/store.rs .sqlx/
git commit -m "feat(store): pick_recent_addition — window 内ランダム 1 件 (最小ケース)"
```

---

### Task 3: window 外を除外するテストと挙動

**Files:**
- Modify: `src/server/store.rs`

- [ ] **Step 1: 失敗テストを追加**

`src/server/store.rs` のテストモジュールに:

```rust
#[tokio::test]
async fn pick_recent_addition_excludes_rows_older_than_window() {
    use chrono::{Duration, Utc};
    let pool = setup_pool().await;
    let repo = RecommendationRepo::new(pool.clone());
    let (saved, _) = repo.upsert(sample_with(
        "rokinon", "a", "Aldous Harding", Some("Train on the Island"),
        Some("https://open.spotify.com/album/A"),
        NaiveDate::from_ymd_opt(2026, 5, 1).unwrap(),
    )).await.unwrap();
    // window 外 (100 日前) に置く
    set_created_at(&pool, saved.id, Utc::now() - Duration::days(100)).await;

    let since = Utc::now() - Duration::days(30);
    assert!(repo.pick_recent_addition(since).await.unwrap().is_none());
}
```

- [ ] **Step 2: テストを実行 (パスする想定)**

Task 2 で `HAVING MIN(created_at) >= ?` を既に書いているため、 このテストは追加実装なしでパスする。

Run: `cargo test --features ssr store::tests::pick_recent_addition_excludes_rows_older_than_window`
Expected: PASS

(もし fail した場合は SQL の HAVING 句を確認し、 必要なら修正)

- [ ] **Step 3: コミット**

```bash
git add src/server/store.rs
git commit -m "test(store): pick_recent_addition は window 外を除外する"
```

---

### Task 4: dedup group は MIN(created_at) で判定するテスト

**Files:**
- Modify: `src/server/store.rs`

- [ ] **Step 1: 失敗テストを追加**

```rust
#[tokio::test]
async fn pick_recent_addition_excludes_dedup_group_when_oldest_row_is_outside_window() {
    use chrono::{Duration, Utc};
    let pool = setup_pool().await;
    let repo = RecommendationRepo::new(pool.clone());
    let url = "https://open.spotify.com/album/shared";

    // 同 dedup_key (同じ spotify_url) の 2 行。 古い方が window 外、 新しい方が window 内
    let (old_row, _) = repo.upsert(sample_with(
        "rokinon", "old", "Foo", Some("Bar"),
        Some(url),
        NaiveDate::from_ymd_opt(2026, 4, 1).unwrap(),
    )).await.unwrap();
    set_created_at(&pool, old_row.id, Utc::now() - Duration::days(100)).await;

    let (new_row, _) = repo.upsert(sample_with(
        "pitchfork", "new", "Foo", Some("Bar"),
        Some(url),
        NaiveDate::from_ymd_opt(2026, 5, 1).unwrap(),
    )).await.unwrap();
    set_created_at(&pool, new_row.id, Utc::now() - Duration::days(5)).await;

    let since = Utc::now() - Duration::days(30);
    // group の MIN(created_at) = -100 日 で window 外 → 除外
    assert!(repo.pick_recent_addition(since).await.unwrap().is_none(),
        "group の MIN で判定するため、 新しい sibling があっても拾われない");
}
```

- [ ] **Step 2: テストを実行 (パスする想定)**

Run: `cargo test --features ssr store::tests::pick_recent_addition_excludes_dedup_group_when_oldest_row_is_outside_window`
Expected: PASS (Task 2 の SQL が GROUP BY + MIN ベースなので、 仕様通り動く)

- [ ] **Step 3: コミット**

```bash
git add src/server/store.rs
git commit -m "test(store): dedup group は MIN(created_at) で window 判定する"
```

---

### Task 5: 同一 group の head 行 + 別フィールドの coalesce テスト

**Files:**
- Modify: `src/server/store.rs`

- [ ] **Step 1: 失敗テストを追加**

```rust
#[tokio::test]
async fn pick_recent_addition_coalesces_optional_fields_across_sources_in_same_group() {
    use chrono::{Duration, Utc};
    let pool = setup_pool().await;
    let repo = RecommendationRepo::new(pool.clone());
    let url = "https://open.spotify.com/album/coalesced";

    // 古い row (featured_at が古い) が youtube_url を持ち、 新しい row は None
    let mut older = sample_with(
        "rokinon", "r1", "Foo", Some("Bar"),
        Some(url),
        NaiveDate::from_ymd_opt(2026, 4, 1).unwrap(),
    );
    older.youtube_url = Some("https://youtu.be/old".into());
    let (older_row, _) = repo.upsert(older).await.unwrap();
    set_created_at(&pool, older_row.id, Utc::now() - Duration::days(20)).await;

    let mut newer = sample_with(
        "pitchfork", "p1", "Foo", Some("Bar"),
        Some(url),
        NaiveDate::from_ymd_opt(2026, 5, 8).unwrap(),
    );
    newer.youtube_url = None;
    let (newer_row, _) = repo.upsert(newer).await.unwrap();
    set_created_at(&pool, newer_row.id, Utc::now() - Duration::days(5)).await;

    let since = Utc::now() - Duration::days(30);
    let card = repo.pick_recent_addition(since).await.unwrap().unwrap();

    // head は featured_at の新しい方 (pitchfork = newer) なのでアーティスト名はそこから
    assert_eq!(card.artist_name, "Foo");
    // youtube_url は head が None でも、 sibling から coalesce される
    assert_eq!(card.youtube_url.as_deref(), Some("https://youtu.be/old"));
}
```

- [ ] **Step 2: テスト実行 (Task 2 で実装済みなのでパスする想定)**

Run: `cargo test --features ssr store::tests::pick_recent_addition_coalesces_optional_fields_across_sources_in_same_group`
Expected: PASS

- [ ] **Step 3: コミット**

```bash
git add src/server/store.rs
git commit -m "test(store): pick_recent_addition は dedup group 内で optional フィールドを coalesce"
```

---

### Task 6: View 型 `SelectorCardView` と `From` impl

**Files:**
- Modify: `src/pages/home.rs`

- [ ] **Step 1: 失敗テストを書く**

`src/pages/home.rs` の `#[cfg(test)] mod tests` ブロックに追加:

```rust
#[cfg(feature = "ssr")]
#[test]
fn selector_card_view_from_domain_formats_added_at_as_yyyy_mm_dd() {
    use chrono::{TimeZone, Utc};
    use crate::domain::selector_card::SelectorCard;

    let card = SelectorCard {
        artist_name: "Aldous Harding".into(),
        album_name: Some("Train on the Island".into()),
        spotify_url: Some("https://open.spotify.com/album/abc".into()),
        spotify_image_url: None,
        youtube_url: None,
        added_at: Utc.with_ymd_and_hms(2026, 5, 10, 12, 0, 0).unwrap(),
    };
    let view = SelectorCardView::from(card);
    assert_eq!(view.artist_name, "Aldous Harding");
    assert_eq!(view.album_name.as_deref(), Some("Train on the Island"));
    assert_eq!(view.added_at, "2026-05-10");
}
```

- [ ] **Step 2: テスト失敗確認**

Run: `cargo test --features ssr selector_card_view_from_domain_formats_added_at_as_yyyy_mm_dd`
Expected: コンパイルエラー (`SelectorCardView` が未定義)

- [ ] **Step 3: 型と From impl を追加**

`src/pages/home.rs` の `AlbumCardView` 定義の **下に** (既存 `From for AlbumCardView` impl の次) 追加:

```rust
#[derive(Clone, Debug, serde::Serialize, serde::Deserialize)]
pub struct SelectorCardView {
    pub artist_name: String,
    pub album_name: Option<String>,
    pub spotify_url: Option<String>,
    pub spotify_image_url: Option<String>,
    pub youtube_url: Option<String>,
    pub added_at: String, // YYYY-MM-DD
}

#[cfg(feature = "ssr")]
impl From<crate::domain::selector_card::SelectorCard> for SelectorCardView {
    fn from(c: crate::domain::selector_card::SelectorCard) -> Self {
        Self {
            artist_name: c.artist_name,
            album_name: c.album_name,
            spotify_url: c.spotify_url,
            spotify_image_url: c.spotify_image_url,
            youtube_url: c.youtube_url,
            added_at: c.added_at.format("%Y-%m-%d").to_string(),
        }
    }
}
```

- [ ] **Step 4: テストを通す**

Run: `cargo test --features ssr selector_card_view_from_domain_formats_added_at_as_yyyy_mm_dd`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/pages/home.rs
git commit -m "feat(home): SelectorCardView と SelectorCard → View の From impl"
```

---

### Task 7: server fn `selector()` を追加

**Files:**
- Modify: `src/pages/home.rs`

- [ ] **Step 1: server fn を追加**

`src/pages/home.rs` の `list_albums` server fn の下に追加:

```rust
#[server(Selector, "/api")]
pub async fn selector() -> Result<Option<SelectorCardView>, ServerFnError> {
    use crate::server::store::RecommendationRepo;
    use chrono::{Duration, Utc};
    use std::sync::Arc;

    let repo = use_context::<Arc<RecommendationRepo>>()
        .ok_or_else(|| ServerFnError::new("repo missing"))?;
    let since = Utc::now() - Duration::days(30);
    let picked = repo
        .pick_recent_addition(since)
        .await
        .map_err(|e| ServerFnError::new(e.to_string()))?;
    Ok(picked.map(SelectorCardView::from))
}
```

- [ ] **Step 2: コンパイル確認**

Run: `cargo check --features ssr`
Expected: ビルド成功

server fn の単体テストは書かない (`use_context` を要するため統合相当)。 UI 経由で Task 11 で実機確認する。

- [ ] **Step 3: コミット**

```bash
git add src/pages/home.rs
git commit -m "feat(home): selector server fn"
```

---

### Task 8: UI — `SelectorButton` / `SelectorSlot` / `SelectorCard` コンポーネント

**Files:**
- Modify: `src/pages/home.rs`

- [ ] **Step 1: `Home` コンポーネントを修正**

既存 `Home` 関数 (現在 `<header>` と `<Suspense>` を直接 view! に書いている部分) を以下に置き換え:

```rust
#[component]
pub fn Home() -> impl IntoView {
    let cards = Resource::new(|| (), |_| async { list_albums().await });
    let selector_action = Action::new(|_: &()| async { selector().await });

    view! {
        <header class="flex items-baseline justify-between border-b-4 border-double border-ink pb-2 mb-6">
            <h1 class="font-zine italic font-bold text-3xl text-ink m-0">
                "i am rockin on"
            </h1>
            <button
                class="font-zine font-bold text-sm px-3 py-1.5 bg-ink text-paper border border-ink cursor-pointer hover:bg-paper hover:text-ink"
                on:click=move |_| { selector_action.dispatch(()); }
            >
                "Selector"
            </button>
        </header>
        <SelectorSlot action=selector_action/>
        <Suspense fallback=|| view! { <p class="text-sepia">"loading..."</p> }>
            {move || cards.get().map(|r| match r {
                Ok(items) => view! { <AlbumGrid items=items/> }.into_any(),
                Err(e) => view! {
                    <p class="text-err">{format!("error: {e}")}</p>
                }.into_any(),
            })}
        </Suspense>
    }
}
```

- [ ] **Step 2: `SelectorSlot` と `SelectorCard` コンポーネントを追加**

`Home` コンポーネントの **下に** (既存 `AlbumGrid` の上) 追加:

```rust
#[component]
fn SelectorSlot(action: Action<(), Result<Option<SelectorCardView>, ServerFnError>>) -> impl IntoView {
    let pending = action.pending();
    let value = action.value();

    view! {
        {move || {
            if pending.get() {
                view! {
                    <section class="my-6">
                        <p class="font-zine italic text-sepia">"選んどるよ…"</p>
                    </section>
                }.into_any()
            } else {
                match value.get() {
                    None => ().into_any(),
                    Some(Ok(None)) => view! {
                        <section class="my-6">
                            <p class="font-zine italic text-sepia">
                                "直近1ヶ月で追加された一枚はまだないずら"
                            </p>
                        </section>
                    }.into_any(),
                    Some(Ok(Some(card))) => view! {
                        <section class="my-6">
                            <SelectorCard card=card.clone()/>
                            <button
                                class="mt-3 font-zine text-xs px-2.5 py-1 border border-ink text-ink cursor-pointer hover:bg-ink hover:text-paper"
                                on:click=move |_| { action.dispatch(()); }
                            >
                                "もう一度"
                            </button>
                        </section>
                    }.into_any(),
                    Some(Err(e)) => view! {
                        <p class="text-err">{format!("error: {e}")}</p>
                    }.into_any(),
                }
            }
        }}
    }
}

#[component]
fn SelectorCard(card: SelectorCardView) -> impl IntoView {
    let alt = image_alt(&card.artist_name, card.album_name.as_deref());
    view! {
        <article class="bg-card shadow-zine p-4 max-w-md flex flex-col gap-3">
            {match card.spotify_image_url.as_ref() {
                Some(src) => view! {
                    <img
                        class="w-full aspect-square object-cover bg-paper"
                        src=src.clone()
                        alt=alt
                        loading="lazy"
                    />
                }.into_any(),
                None => view! {
                    <div
                        class="w-full aspect-square bg-placeholder flex items-center justify-center text-sepia text-6xl font-zine"
                        aria-hidden="true"
                    >"♪"</div>
                }.into_any(),
            }}
            <div class="flex flex-col gap-1">
                <div class="font-zine font-bold text-lg text-ink leading-tight">
                    {card.artist_name.clone()}
                </div>
                {card.album_name.clone().map(|a| view! {
                    <div class="font-zine italic text-base text-sepia leading-tight">{a}</div>
                })}
            </div>
            <div class="flex flex-wrap gap-2 items-center">
                {card.spotify_url.clone().map(|u| view! {
                    <a
                        class="text-xs font-semibold px-2.5 py-1 rounded-full bg-spotify text-white no-underline"
                        href=u
                        target="_blank"
                        rel="noopener"
                    >"Spotify"</a>
                })}
                {card.youtube_url.clone().map(|u| view! {
                    <a
                        class="text-xs font-semibold px-2.5 py-1 rounded-full bg-youtube text-white no-underline"
                        href=u
                        target="_blank"
                        rel="noopener"
                    >"YouTube"</a>
                })}
                <span class="ml-auto text-[0.7rem] text-sepia">{card.added_at.clone()}</span>
            </div>
        </article>
    }
}
```

- [ ] **Step 3: コンパイル確認**

Run: `cargo check --features ssr && cargo check --features hydrate`
Expected: 両 feature でビルド成功

- [ ] **Step 4: ユニットテストが全件パスすることを確認**

Run: `cargo test --features ssr`
Expected: 既存テスト + Task 1-6 で追加したテスト全件 PASS

- [ ] **Step 5: コミット**

```bash
git add src/pages/home.rs
git commit -m "feat(home): Selector ボタン / Slot / カードコンポーネントを追加"
```

---

### Task 9: 実機 UI 確認 (golden path)

**Files:** なし (browser での目視確認)

- [ ] **Step 1: 開発サーバ起動**

別ターミナルで:
```bash
mise run dev
```

- [ ] **Step 2: golden path を確認**

`agent-browser` 経由で:
```bash
agent-browser open http://127.0.0.1:3000
agent-browser snapshot -i
```

期待: ヘッダ右に `Selector` ボタンが見える。 grid は通常通り表示される。

```bash
agent-browser click @<selector-button-ref>
agent-browser snapshot -i
```

期待: `<section>` が現れて、 1 枚カードが表示される。 「もう一度」 ボタンも見える。

- [ ] **Step 3: empty state 確認 (in-memory で十分なら skip)**

dev DB は常時データありなので、 empty state の実機確認は YAGNI でスキップ可。 store テストでカバー済み。

- [ ] **Step 4: タスクとしてのコミット無し** (実装変更がないため)

問題があれば該当 Task に戻って修正コミット。

---

### Task 10: 視覚回帰テストが (未更新で) 通ることを確認

**Files:** なし

- [ ] **Step 1: cargo leptos build**

Run: `cargo leptos build --features ssr`
Expected: ビルド成功

- [ ] **Step 2: 視覚回帰テスト実行**

Run: `mise run visual`
Expected: 既存 4 テスト全件 PASS (`SelectorSlot` が idle 時 DOM 不在のため、 `ul.tilt-cycle` のカード count = 3 と grid-template-columns count は変わらない)

- [ ] **Step 3: もし fail したら**

`SelectorSlot` の idle 時 view が空ノードを出してしまっている可能性。 `().into_any()` を返しているか確認 — フラグメントタグや空 `<section>` を返すと grid 兄弟要素として DOM に乗り、 視覚レイアウトには影響しないがテスト的に問題はない。 ただし `ul.tilt-cycle > li` の count には影響しないので、 既存テストは通るはず。

- [ ] **Step 4: コミット無し** (テスト変更なし)

---

### Task 11: 仕上げ — full test suite + lint + PR

**Files:** なし

- [ ] **Step 1: full test**

Run: `mise run test`
Expected: 全件 PASS

- [ ] **Step 2: sqlx prepare check (CI と同じチェック)**

Run: `cargo sqlx prepare --check -- --features ssr`
Expected: `.sqlx/` が最新。 ズレてたら `cargo sqlx prepare -- --features ssr` で再生成して commit

- [ ] **Step 3: PR を作成**

```bash
git push -u origin feat/selector-spec
gh pr create --title "feat: Selector — 直近1ヶ月で DB に追加されたアルバムをランダム1枚" --body "$(cat <<'EOF'
ホームに `Selector` ボタンを追加。 dedup group 単位で MIN(created_at) が直近 30 日内のアルバムから RANDOM で 1 枚を抜く。

- store: `pick_recent_addition(since)` を CTE + HAVING MIN(created_at) >= ? で実装
- domain: `SelectorCard` 型 (added_at = MIN(created_at))
- view: `SelectorCardView` 別型 (sources 非保持、 added_at は YYYY-MM-DD 文字列)
- UI: idle 時は DOM 不在で grid の視覚回帰に影響なし
- spec: `docs/superpowers/specs/2026-05-15-selector-design.md`
EOF
)"
```

- [ ] **Step 4: PR 監視**

CI が green になるまで待機。 auto-merge は手動判断。

---

## Self-Review チェック後の確認事項

- spec の機能名・型名 (Selector / SelectorCard / SelectorCardView) と全 Task で一致
- spec で言及している「視覚回帰スナップショット更新」 はプラン冒頭で訂正済み
- 各 Task で具体的なファイル・コード・コマンドを記載、 placeholder なし
- TDD: テスト先 → 失敗確認 → 実装 → 通過確認 → コミット の順
- Task 3-5 は Task 2 の SQL がすでに条件を満たすため 「テスト追加 → そのままパス」 になる構成。 これは TDD の 「設計が正しいことの確認」 として意味あり (回帰防止)
